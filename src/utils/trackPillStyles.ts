/** Pathway tracks → digital ancestor colour tokens (`--guardian`, etc. in global.css). */
export const TRACK_CHARACTER = {
  protect: "guardian",
  admin: "steward",
  create: "trailblazer",
} as const;
